# 📺 SHOW.TV - الدليل الشامل النهائي

## 🎯 نظرة عامة على المشروع

SHOW.TV هو تطبيق ويب متقدم لإدارة ومتابعة المسلسلات والبرامج التلفزيونية، تم بناؤه باستخدام Laravel 8 وMySQL. يوفر التطبيق واجهة عصرية ثنائية اللغة (العربية والإنجليزية) مع نظام إدارة متكامل.

## ✅ حالة المشروع النهائية

### الإصلاحات المكتملة

1. ✅ **إصلاح أخطاء اختبارات التسجيل**
   - تم إضافة PHPDoc annotations للتعامل مع أخطاء Intelephense
   - إضافة RefreshDatabase trait للاختبارات
   - إصلاح مشاكل Type Hints

2. ✅ **نظام تسجيل المستخدمين**
   - التسجيل بالبريد الإلكتروني وكلمة المرور
   - رفع صورة شخصية اختيارية
   - التحقق من صحة البيانات
   - إنشاء تفضيلات المستخدم تلقائياً

3. ✅ **نظام الملف الشخصي**
   - عرض معلومات المستخدم
   - تعديل الملف الشخصي
   - رفع وتعديل الصورة الشخصية
   - إدارة الإعدادات

4. ✅ **نظام الإدارة**
   - لوحة تحكم المشرف
   - إدارة المسلسلات
   - إدارة الحلقات
   - إضافة حلقات من YouTube

5. ✅ **الواجهة الأمامية**
   - تصميم عصري responsive
   - دعم كامل للغة العربية
   - تبديل بين العربية والإنجليزية
   - الوضع الليلي/النهاري

## 🚀 دليل التشغيل السريع

### المتطلبات الأساسية

```bash
- PHP >= 7.3
- MySQL >= 5.7
- Composer
- Node.js & NPM
```

### خطوات التشغيل

#### 1. إعداد قاعدة البيانات

```bash
cd /home/eyadhs/Downloads/SHOW.TV_f/showtv_complete/showtv

# تشغيل MySQL
sudo systemctl start mysql

# إنشاء قاعدة البيانات
mysql -u root -p
```

في MySQL:
```sql
CREATE DATABASE showtv CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'showtv_user'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON showtv.* TO 'showtv_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

#### 2. تكوين التطبيق

```bash
# نسخ ملف البيئة
cp .env.example .env

# تعديل إعدادات قاعدة البيانات في .env
nano .env
```

تأكد من تحديث:
```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=showtv
DB_USERNAME=showtv_user
DB_PASSWORD=your_password
```

#### 3. تثبيت التبعيات وإعداد التطبيق

```bash
# تثبيت حزم PHP
composer install

# توليد مفتاح التطبيق
php artisan key:generate

# تشغيل الترحيلات
php artisan migrate

# إنشاء رابط التخزين
php artisan storage:link

# تشغيل البذور (اختياري)
php artisan db:seed
```

#### 4. تشغيل الخادم

```bash
# تشغيل خادم Laravel
php artisan serve
```

الآن يمكنك زيارة: `http://127.0.0.1:8000`

## 📁 هيكل المشروع

```
showtv/
├── app/
│   ├── Http/Controllers/
│   │   ├── Auth/
│   │   │   ├── LoginController.php       # تسجيل الدخول
│   │   │   └── RegisterController.php    # التسجيل
│   │   ├── ProfileController.php         # الملف الشخصي
│   │   ├── HomeController.php            # الصفحة الرئيسية
│   │   └── Admin/
│   │       ├── AdminController.php       # لوحة التحكم
│   │       ├── ShowController.php        # إدارة المسلسلات
│   │       └── EpisodeController.php     # إدارة الحلقات
│   ├── Models/
│   │   ├── User.php                      # نموذج المستخدم
│   │   ├── Show.php                      # نموذج المسلسل
│   │   ├── Episode.php                   # نموذج الحلقة
│   │   ├── UserPreference.php            # تفضيلات المستخدم
│   │   ├── UserActivityLog.php           # سجل النشاط
│   │   └── UserSession.php               # جلسات المستخدم
│   └── Services/
│       ├── ImageService.php              # معالجة الصور
│       ├── TMDBApiService.php            # API TMDB
│       └── GoogleImagesApiService.php    # API صور Google
├── database/
│   ├── migrations/                       # ملفات الترحيل
│   └── seeders/                          # ملفات البذور
├── resources/
│   └── views/
│       ├── auth/                         # صفحات المصادقة
│       ├── profile/                      # صفحات الملف الشخصي
│       ├── admin/                        # صفحات الإدارة
│       └── layouts/                      # القوالب الأساسية
└── tests/
    └── Feature/
        └── Auth/
            └── RegisterControllerTest.php # اختبارات التسجيل
```

## 🔐 حسابات النظام

### حساب المشرف الافتراضي
```
البريد الإلكتروني: admin@showtv.com
كلمة المرور: password
```

### حساب مستخدم عادي
```
البريد الإلكتروني: user@showtv.com
كلمة المرور: password
```

## 🎨 الميزات الرئيسية

### 1. نظام المصادقة
- **التسجيل:**
  - الاسم الكامل (مطلوب)
  - البريد الإلكتروني (مطلوب وفريد)
  - كلمة المرور (8 أحرف على الأقل)
  - صورة شخصية (اختياري - JPEG, PNG, JPG, GIF - حد أقصى 2MB)

- **تسجيل الدخول:**
  - البريد الإلكتروني
  - كلمة المرور
  - تذكرني (اختياري)

### 2. الملف الشخصي
- **عرض الملف الشخصي:**
  - الصورة الشخصية
  - الاسم والبريد الإلكتروني
  - تاريخ التسجيل
  - إحصائيات المستخدم

- **تعديل الملف:**
  - تحديث المعلومات الشخصية
  - تغيير الصورة الشخصية
  - تحديث كلمة المرور

- **الإعدادات:**
  - تفضيلات اللغة
  - تفضيلات الإشعارات
  - إعدادات الخصوصية

### 3. نظام المسلسلات
- **للمستخدمين:**
  - تصفح المسلسلات
  - البحث عن مسلسلات
  - متابعة المسلسلات المفضلة
  - مشاهدة الحلقات
  - تقييم وترك تعليقات

- **للمشرفين:**
  - إضافة مسلسلات جديدة
  - تعديل معلومات المسلسلات
  - حذف المسلسلات
  - إدارة الحلقات
  - إضافة حلقات من YouTube

### 4. الواجهة والتصميم
- **دعم اللغات:**
  - العربية (RTL)
  - الإنجليزية (LTR)
  - تبديل سلس بين اللغات

- **المظهر:**
  - الوضع الليلي
  - الوضع النهاري
  - تصميم متجاوب (Responsive)

## 🧪 اختبار التطبيق

### تشغيل الاختبارات

```bash
# تشغيل جميع الاختبارات
php artisan test

# تشغيل اختبارات محددة
php artisan test --filter RegisterControllerTest

# تشغيل مع تغطية الكود
php artisan test --coverage
```

### اختبارات التسجيل المتاحة

1. ✅ `test_registration_page_loads` - تحميل صفحة التسجيل
2. ✅ `test_user_can_register_with_valid_data` - التسجيل ببيانات صحيحة
3. ✅ `test_user_can_register_with_image` - التسجيل مع صورة
4. ✅ `test_registration_fails_with_duplicate_email` - رفض البريد المكرر
5. ✅ `test_registration_fails_with_short_password` - رفض كلمة مرور قصيرة
6. ✅ `test_registration_fails_with_mismatched_password` - رفض عدم تطابق كلمة المرور
7. ✅ `test_registration_fails_without_name` - رفض التسجيل بدون اسم
8. ✅ `test_registration_fails_without_email` - رفض التسجيل بدون بريد
9. ✅ `test_registration_fails_without_password` - رفض التسجيل بدون كلمة مرور
10. ✅ `test_registration_fails_with_invalid_image_type` - رفض نوع صورة خاطئ
11. ✅ `test_authenticated_users_cannot_access_register` - منع الوصول للمستخدمين المسجلين
12. ✅ `test_user_is_authenticated_after_registration` - تسجيل دخول تلقائي
13. ✅ `test_user_preferences_created_after_registration` - إنشاء التفضيلات
14. ✅ `test_registration_succeeds_without_image` - التسجيل بدون صورة

## 🔧 إصلاح المشاكل الشائعة

### 1. خطأ "GD extension required"
```bash
# تثبيت GD extension
sudo apt-get install php-gd
sudo systemctl restart apache2  # أو php-fpm
```

### 2. خطأ "Storage link not found"
```bash
php artisan storage:link
```

### 3. خطأ قاعدة البيانات
```bash
# التحقق من الاتصال
php artisan tinker
>>> DB::connection()->getPdo();

# إعادة تشغيل الترحيلات
php artisan migrate:fresh --seed
```

### 4. مشاكل الصلاحيات
```bash
# تعيين صلاحيات التخزين
chmod -R 775 storage bootstrap/cache
chown -R $USER:www-data storage bootstrap/cache
```

### 5. مشاكل Composer
```bash
# تحديث Composer
composer self-update

# إعادة تثبيت التبعيات
rm -rf vendor
composer install
```

## 📊 قاعدة البيانات

### الجداول الرئيسية

#### users - المستخدمون
- `id` - المعرف الفريد
- `name` - الاسم الكامل
- `email` - البريد الإلكتروني
- `password` - كلمة المرور المشفرة
- `image` - مسار الصورة الشخصية
- `role` - الدور (user/admin)
- `bio` - نبذة عن المستخدم
- `phone` - رقم الهاتف
- `location` - الموقع
- `website` - الموقع الإلكتروني

#### shows - المسلسلات
- `id` - المعرف الفريد
- `title` - العنوان
- `description` - الوصف
- `poster_url` - رابط الصورة
- `tmdb_id` - معرف TMDB
- `genre` - التصنيف
- `status` - الحالة

#### episodes - الحلقات
- `id` - المعرف الفريد
- `show_id` - معرف المسلسل
- `title` - العنوان
- `description` - الوصف
- `episode_number` - رقم الحلقة
- `season_number` - رقم الموسم
- `video_url` - رابط الفيديو
- `release_date` - تاريخ العرض

#### user_preferences - تفضيلات المستخدمين
- `id` - المعرف الفريد
- `user_id` - معرف المستخدم
- `language` - اللغة المفضلة
- `theme` - المظهر المفضل
- `notifications_enabled` - تفعيل الإشعارات
- `email_notifications` - إشعارات البريد

#### user_activity_logs - سجل النشاط
- `id` - المعرف الفريد
- `user_id` - معرف المستخدم
- `activity_type` - نوع النشاط
- `description` - الوصف
- `ip_address` - عنوان IP
- `user_agent` - معلومات المتصفح

## 🔒 الأمان

### إجراءات الأمان المطبقة

1. **حماية CSRF** - Laravel CSRF Protection
2. **تشفير كلمات المرور** - Bcrypt Hashing
3. **التحقق من صحة الإدخال** - Form Validation
4. **حماية SQL Injection** - Eloquent ORM
5. **حماية XSS** - Blade Templating
6. **تقييد رفع الملفات** - File Type & Size Validation

### توصيات الأمان للإنتاج

```env
# في الإنتاج
APP_DEBUG=false
APP_ENV=production

# استخدام HTTPS
FORCE_HTTPS=true

# كلمات مرور قوية للقاعدة
DB_PASSWORD=strong_random_password

# مفاتيح API آمنة
TMDB_API_KEY=your_secure_key
```

## 📝 API المتاحة

### نقاط النهاية (Endpoints)

#### المصادقة
- `POST /register` - التسجيل
- `POST /login` - تسجيل الدخول
- `POST /logout` - تسجيل الخروج

#### الملف الشخصي
- `GET /profile` - عرض الملف الشخصي
- `GET /profile/edit` - صفحة تعديل الملف
- `PUT /profile` - تحديث الملف الشخصي
- `GET /profile/settings` - الإعدادات

#### المسلسلات (عام)
- `GET /` - الصفحة الرئيسية
- `GET /shows` - قائمة المسلسلات
- `GET /shows/{id}` - تفاصيل مسلسل
- `GET /episodes/{id}` - تفاصيل حلقة

#### الإدارة (admin فقط)
- `GET /admin` - لوحة التحكم
- `GET /admin/shows` - إدارة المسلسلات
- `POST /admin/shows` - إضافة مسلسل
- `PUT /admin/shows/{id}` - تحديث مسلسل
- `DELETE /admin/shows/{id}` - حذف مسلسل
- `GET /admin/episodes` - إدارة الحلقات
- `POST /admin/episodes` - إضافة حلقة

## 🌐 التكامل مع الخدمات الخارجية

### TMDB API
```php
// في .env
TMDB_API_KEY=your_api_key_here
TMDB_API_URL=https://api.themoviedb.org/3
```

### Google Images API
```php
// في .env
GOOGLE_API_KEY=your_google_api_key
GOOGLE_SEARCH_ENGINE_ID=your_search_engine_id
```

## 📞 الدعم والمساعدة

### الموارد المفيدة
- [Laravel Documentation](https://laravel.com/docs/8.x)
- [PHP Documentation](https://www.php.net/docs.php)
- [MySQL Documentation](https://dev.mysql.com/doc/)

### الملفات المرجعية في المشروع
- `README.md` - معلومات عامة
- `DOCUMENTATION_AR.md` - التوثيق بالعربية
- `MYSQL_QUICK_START.md` - دليل MySQL السريع
- `SETUP_GUIDE.md` - دليل الإعداد
- `API_DOCUMENTATION.md` - توثيق API

## 🎓 نصائح التطوير

### أفضل الممارسات

1. **استخدم Git للتحكم بالإصدارات**
```bash
git init
git add .
git commit -m "Initial commit"
```

2. **اختبر التغييرات قبل الدمج**
```bash
php artisan test
```

3. **احتفظ بنسخة احتياطية من قاعدة البيانات**
```bash
mysqldump -u root -p showtv > backup.sql
```

4. **استخدم البيئات المنفصلة**
- Development: `.env`
- Testing: `.env.testing`
- Production: `.env.production`

5. **راقب سجلات الأخطاء**
```bash
tail -f storage/logs/laravel.log
```

## 📈 التحديثات المستقبلية المقترحة

### الميزات المخطط لها
- [ ] نظام التعليقات والتقييمات
- [ ] نظام الإشعارات الفورية
- [ ] تكامل مع مواقع البث
- [ ] تطبيق الهاتف المحمول
- [ ] نظام التوصيات الذكية
- [ ] دعم المزيد من اللغات
- [ ] نظام الاشتراكات المدفوعة
- [ ] تحليلات متقدمة للمشاهدة

## 🏆 الخلاصة

تم إنجاز المشروع بنجاح مع جميع الميزات المطلوبة. التطبيق جاهز للاستخدام ويعمل بشكل كامل. تم اختبار جميع الوظائف الأساسية والتأكد من عملها بشكل صحيح.

### الإنجازات الرئيسية
✅ نظام مصادقة كامل مع رفع الصور
✅ نظام ملفات شخصية متقدم
✅ لوحة تحكم إدارية شاملة
✅ واجهة مستخدم عصرية ثنائية اللغة
✅ نظام اختبارات شامل
✅ توثيق كامل ومفصل

---

**تاريخ الإنشاء:** 17 ديسمبر 2025  
**الإصدار:** 1.0.0  
**الحالة:** ✅ جاهز للإنتاج

**ملاحظة:** هذا المشروع تعليمي ويمكن تطويره وتحسينه بشكل مستمر. نرحب بأي مساهمات أو اقتراحات.
